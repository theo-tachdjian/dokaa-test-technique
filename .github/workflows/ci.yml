name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  backend-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install backend dependencies
        run: npm ci
        working-directory: ./Projet - Dokaa/server
      - name: Run backend tests
        run: npm test
        working-directory: ./Projet - Dokaa/server
        env:
          DISABLE_SCRAPING: 'true'

  frontend-build-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install frontend dependencies
        run: npm ci
        working-directory: ./Projet - Dokaa/client
      - name: Run frontend lint
        run: npm run lint --if-present
        working-directory: ./Projet - Dokaa/client
      - name: Build frontend
        run: npm run build --if-present
        working-directory: ./Projet - Dokaa/client

